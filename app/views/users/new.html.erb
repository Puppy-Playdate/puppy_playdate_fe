<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create an Account</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      text-align: center;
      background-color: #f5f5f5;
      margin: 0;
      padding: 20px;
    }

    #create {
      max-width: 400px;
      margin: 0 auto;
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    h1 {
      color: #e44d26;
      font-weight: bold;
      margin-bottom: 20px;
    }

    label {
      display: block;
      margin-bottom: 8px;
      color: #333;
    }

    input {
      width: 100%;
      padding: 10px;
      margin-bottom: 16px;
      box-sizing: border-box;
      border: 1px solid #ddd;
      border-radius: 4px;
      font-size: 16px;
    }

    input[type="submit"] {
      background-color: #4285f4;
      color: #fff;
      border: none;
      padding: 12px 16px;
      font-size: 16px;
      cursor: pointer;
      border-radius: 4px;
    }
  </style>
</head>
<body>
<br/>
<div id="create">
  <h1>Create an Account</h1>
  <%= form_with url: "/users", method: :post do |f| %>
    <label for="name">Name:</label>
    <%= f.text_field :name, required: true, id: "name" %>
    <br/>
    <label for="email">Email:</label>
    <%= f.text_field :email, required: true, id: "email" %>
    <br/>
    <label for="password">Password:</label>
    <%= f.password_field :password, required: true, id: "password" %>
    <br/>
    <label for="password_confirmation">Confirm Password:</label>
    <%= f.password_field :password_confirmation, required: true, id: "password_confirmation" %>
    <br/><br/>
    <%= f.submit "Create New User" %>
  <% end %>
</div>

</body>
</html>
