<h1>You've made it to the dashboard</h1>

<h3>Name: <%= @user.name %></h3>
<p>Email: <%= @user.email %></p><h2><%= @user.name %>'s Dashboard</h2>

<div id="dashboard_nav" >
  <p><%= button_to "Edit Profile", edit_user_path(@user.user_id), method: :get %></p>
  <p><%= button_to "Add Pet", new_user_dog_path(@user.user_id), method: :get  %></p>
  <p><%= button_to "View Pets", user_dogs_path(@user.user_id), method: :get  %></p>
  <p><%= button_to "Discover Socials", user_socials_path(@user.user_id), method: :get %></p>
</div>  