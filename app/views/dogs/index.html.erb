<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= "#{@user.name}'s Dogs" %></title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      text-align: center;
      background-color: #f5f5f5;
      margin: 0;
      padding: 20px;
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
    }

    .dog-container {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
    }

    .dog-box {
      border: 1px solid #ddd;
      padding: 15px;
      text-align: left;
      width: 200px;
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
    }

    .dog-box p {
      margin: 0;
      color: #555;
    }

    .edit-button {
      display: inline-block;
      margin-top: 10px;
      background-color: #4285f4;
      color: #fff;
      border: none;
      padding: 8px 12px;
      font-size: 14px;
      cursor: pointer;
      border-radius: 4px;
    }

    .add-dog-button {
      background-color: #34a853;
      color: #fff;
      border: none;
      padding: 12px 16px;
      font-size: 16px;
      cursor: pointer;
      border-radius: 4px;
    }
  </style>
</head>
<body>

<h1><%= "#{@user.name}'s Dogs" %></h1>

<div class="dog-container">
  <% @dogs_facade.each do |dog| %>
    <div class="dog-box">
      <p><b>Name: </b><%= dog.name %></p><br/>
      <p><b>Breed: </b><%= dog.breed %></p><br/>
      <p><b>Age: </b><%= dog.age %></p><br/>
      <p><b>Size: </b><%= dog.size %></p><br/>
      <p><b>Neutered: </b><%= dog.neutered ? 'true' : 'false' %></p><br/>
      <%= button_to "Edit #{dog.name}", edit_user_dog_path(@user.user_id, dog.dog_id), class: 'edit-button' %>
    </div>
  <% end %>
</div>

<br/><br/>
<%= button_to "Add New Dog", new_user_dog_path(@user.user_id), method: :get, class: 'add-dog-button' %>

</body>
</html>