<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      text-align: center; 
    }

    h1 {
      margin-top: 20px; 
    }

    .dog-container {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px; 
      margin-top: 20px; 
    }

    .dog-box {
      border: 1px solid #ddd;
      padding: 10px;
      text-align: left;
      width: 200px; 
    }
  </style>
</head>
<body>

<h1><%= "#{@user.name}'s Dogs" %></h1>

<div class="dog-container">
  <% @dogs_facade.each do |dog| %>
    <div class="dog-box">
      <p>Name: <%= dog.name %></p>
      <p>Breed: <%= dog.breed %></p>
      <p>Age: <%= dog.age %></p>
      <p>Size: <%= dog.size %></p>
      <p>Neutered: <%= dog.neutered ? 'true' : 'false' %></p>
      <!-- Add photo display logic here if we decide to -->
      <%= button_to "Edit Dog", edit_dog_path(@user) %>
    </div>
  <% end %>
</div>

<%= button_to "Add Dog", add_dog_path(@user) %>

</body>
</html>
