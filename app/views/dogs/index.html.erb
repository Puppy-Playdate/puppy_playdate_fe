<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      text-align: center; 
    }

    h1 {
      margin-top: 20px; 
      color: red;
      font_weight: bold;
    }

    .dog-container {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px; 
      margin-top: 20px; 
    }

    .dog-box {
      border: 5px solid #ddd;
      padding: 10px;
      text-align: left;
      width: 200px; 
    }
  </style>
</head>
<body>

<h1><%= "#{@user.name}'s Dogs" %></h1>

<div class="dog-container">
  <% @dogs_facade.each do |dog| %>
    <div class="dog-box">
      <p>Name: <%= dog.name %></p>
      <p>Breed: <%= dog.breed %></p>
      <p>Age: <%= dog.age %></p>
      <p>Size: <%= dog.size %></p>
      <p>Neutered: <%= dog.neutered ? 'true' : 'false' %></p>
      <!-- Add photo display logic here if we decide to -->
      <%= button_to "Edit #{dog.name}", edit_user_dog_path(@user.user_id, dog.dog_id) %>
    </div>
  <% end %>
</div>

<%= button_to "Add New Dog", new_user_dog_path(@user.user_id), method: :get %>

</body>
</html>
