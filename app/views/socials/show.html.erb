<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Social Details</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      text-align: center;
      background-color: #f5f5f5;
      margin: 0;
      padding: 20px;
    }

    h1, p {
      color: #333;
    }

    #social_details {
      max-width: 600px;
      margin: 0 auto;
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    #google_map {
      margin-top: 20px;
      max-width: 800px;
      margin: 0 auto;
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    iframe {
      width: 100%;
      height: 400px;
      border: none;
    }

    button {
      background-color: #4285f4;
      color: #fff;
      border: none;
      padding: 12px 16px;
      font-size: 16px;
      cursor: pointer;
      border-radius: 4px;
    }

    button:hover {
      background-color: #3a76e7;
    }
  </style>
</head>
<body>
<br/>
<div id="social_details">
  <p>Name: <h1><%= @social.name %></h1></p>
  <p><b>Description: </b><%= @social.description %></p>
  <p><b>Event Type: </b><%= @social.event_type %></p>
  <p><b>Event Date: </b><%= @social.event_date %></p>
  <p><b>Host User: </b><%= @host.name %></p>
  <%= button_to "Edit Social", edit_user_social_path(@host.user_id, @social.id) %>
</div>

<div id="google_map" class="map_container">
  <h1>Find on Maps</h1>
  <iframe
      frameborder="0" style="border:0"
      referrerpolicy="no-referrer-when-downgrade"
      src="https://www.google.com/maps/embed/v1/place?key=<%= Rails.application.credentials.google[:maps_api] %>&q=<%= @social.location %>&zoom=13&maptype=satellite"
      allowfullscreen>
  </iframe>
</div>

</body>
</html>